import { WorkspacesPage } from "@/components/app/workspaces/workspaces-page";

export const metadata = {
  title: "Workspaces — NexTask",
};

export default function WorkspacesRoute() {
  return <WorkspacesPage />;
}
